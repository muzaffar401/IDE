# Environment Configuration
NODE_ENV=development
PORT=5000

# Database Configuration
# The app uses in-memory storage by default, so this can be a placeholder
DATABASE_URL=postgresql://localhost:5432/cursorweb_dev